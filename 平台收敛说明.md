# Z Ai 平台收敛说明

> 变更日期：2026-09-16
> 变更内容：项目由「Android / iOS / Windows / macOS / Linux」五端收敛为 **仅支持 Android + Windows**

---

## 一、变更概览

| 平台 | 原状态 | 现状态 |
|------|--------|--------|
| Android | ✅ 完整支持 | ✅ 完整支持（含前台服务通知） |
| Windows | ✅ 支持（托盘） | ✅ 支持（托盘） |
| iOS | ✅ 支持 | ❌ 已移除 |
| macOS | ✅ 支持 | ❌ 已移除 |
| Linux | ✅ 支持 | ❌ 已移除 |
| Web | ❌ 已放弃 | ❌ 已放弃 |

---

## 二、代码改动

### 1. 平台能力检测 `lib/core/platform/platform_capabilities.dart`
- 删除 `isMacOS` / `isLinux` / `isIOS` getter
- `isDesktop` → `Platform.isWindows`
- `isMobile` → `Platform.isAndroid`
- `supportsSystemTray` → `isDesktop`（即 Windows）
- `supportsForegroundService` → `Platform.isAndroid`

### 2. 前台服务 `lib/core/platform/foreground_service_impl.dart`
- `isSupported` 仅判断 `TargetPlatform.android`

### 3. 系统托盘 `lib/core/platform/system_tray_impl.dart`
- `isSupported` 仅判断 `Platform.isWindows`

### 4. 图片工具 `lib/utils/image_utils.dart`
- 保存到相册仅支持 Android（iOS 分支移除）
- 注释更新为「仅 Android / Windows」

### 5. 系统托盘管理器 `lib/utils/system_tray.dart`
- 图标路径固定使用 `.ico`（Windows）
- 移除 png 分支与 `dart:io` import

---

## 三、资源与配置改动

### 1. 删除的平台目录
- `ios/`（Xcode 项目）
- `macos/`（macOS Runner）
- `linux/`（Linux Runner）

### 2. 托盘图标
- `assets/icons/` 精简为 4 个 `.ico`：`tray_idle.ico` / `tray_loading.ico` / `tray_completed.ico` / `tray_error.ico`
- `tool/generate_tray_icons.py` 只生成 ICO 格式

### 3. pubspec.yaml
- 移除 iOS 版本说明注释
- 托盘注释改为「系统托盘（Windows）」

### 4. analysis_options.yaml
- `exclude` 仅保留 `build/**` / `android/**` / `windows/**`

### 5. .metadata
- 移除 ios / linux / macos 平台记录

---

## 四、文档更新

- `README.md` / `README_EN.md`：支持平台表、构建命令、技术栈
- `CODE_WIKI.md`：项目概述、目录结构、平台适配表、运行/构建命令
- `设计方案.md`：项目概述
- `重制方案.md`：执行进度表新增平台收敛决策记录

---

## 五、验证

- ✅ `flutter analyze`：**No issues found**
- ✅ 项目目录仅剩 `android/`、`windows/` 平台目录

> 注意：`flutter pub get` 会自动把已删除平台的 exclude 加回 `analysis_options.yaml`，
> 如再次出现 `ios/**` / `macos/**` / `linux/**`，可手动删除（不影响构建）。