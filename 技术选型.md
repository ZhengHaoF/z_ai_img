# Z Ai Web — 技术选型

> 配套《功能清单（网页版）》。目标：Node 服务端扛长请求 + 浏览器提交/轮询任务。
> 决策日期：2026-09-16

---

## 1. 总览

| 层 | 选型 | 版本建议 | 说明 |
|----|------|----------|------|
| 运行时 | Node.js | 20 LTS+ | 单进程部署 |
| HTTP 框架 | **Express** | ^4 / ^5 | 中间件生态成熟，团队熟悉成本低 |
| 任务/历史存储 | SQLite | `better-sqlite3` | 单机落盘，扛进程重启 |
| 图片存储 | 本地磁盘 `data/images/` | — | 预览/下载直接读文件 |
| 上游 HTTP | 原生 `fetch` | Node 20+ | 调 Images API；multipart 用 `FormData` |
| 配置 | `.env`（密钥）+ `config.json`（多 Profile） | — | Key 只在服务端；结构可入库 |
| 前端构建 | **Vite** | ^6 | 开发快，产出纯静态 |
| 前端框架 | **Vue 3** | ^3.5 | 单页应用（Composition API） |
| 前端语言 | TypeScript | ^5 | 与 Vite/Vue 官方栈一致 |
| 状态 | Pinia（或极简 composables） | ^2 | 任务表单/轮询状态够用即可 |
| 样式 | 原生 CSS / 少量工具类 | — | 不绑 UI 库；需要再引 Naive/Element |
| 任务轮询 | `fetch` + 间隔 2–3s | — | 首版不用 WebSocket；接口预留 SSE |
| 进程守护 | `pm2` 或 systemd | — | 服务器部署可选 |

**明确不用（首版）**

| 技术 | 原因 |
|------|------|
| NestJS / 微服务 | 单应用过重 |
| Redis / BullMQ | 单机并发低；接口按任务表设计，后续可换 |
| PostgreSQL / MySQL | SQLite 足够；数据量大再迁 |
| Next.js / Nuxt SSR | 无 SEO 硬需求，纯 SPA 即可 |
| Preact | 已定 Vue 3；若要极致体积再评估 |
| 浏览器直连上游 | CORS + Key 泄露；必须经 Node 代理 |

---

## 2. 仓库与目录结构（建议）

```
z-ai-web/
├── server/                 # Express
│   ├── src/
│   │   ├── index.js        # 入口、静态托管、路由挂载
│   │   ├── routes/tasks.js # POST/GET /api/tasks…
│   │   ├── services/
│   │   │   ├── imageApi.js # 上游 generations / edits 适配（双协议）
│   │   │   └── taskRunner.js
│   │   ├── db.js           # better-sqlite3 初始化与迁移
│   │   └── config.js       # 读 .env
│   ├── data/               # sqlite + images（gitignore）
│   └── package.json
├── web/                    # Vite + Vue 3 + TS
│   ├── src/
│   │   ├── main.ts
│   │   ├── App.vue
│   │   ├── pages/Generate.vue
│   │   ├── pages/Edit.vue
│   │   ├── pages/Tasks.vue      # 可选：任务历史
│   │   ├── api/client.ts        # /api/tasks 封装
│   │   └── stores/task.ts
│   ├── index.html
│   └── package.json
├── 功能清单.md
└── 技术选型.md
```

开发时 Vite 代理 `/api` 与 `/files` 到 Express；生产由 Express 托管 `web/dist`，同源无 CORS。

---

## 3. 核心接口约定

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/tasks` | 提交任务，立即返回 `{ id, status: "queued" }` |
| GET | `/api/tasks/:id` | `{ status, images?: string[], error? }` |
| POST | `/api/tasks/:id/cancel` | 尽力取消 |
| GET | `/api/tasks` | （可选）最近任务列表 |
| GET | `/files/:name` | 图片预览/下载 |
| GET | `/api/profiles` | 可用配置集（无密钥） |

**任务状态机**：`queued → running → succeeded | failed | canceled`

**POST body（生成）**

```json
{
  "type": "generate",
  "profileId": "qwen",
  "prompt": "…",
  "model": "qwen-image-3.0-pro",
  "size": "1024x1024",
  "n": 1
}
```

**POST body（编辑）**：`type: "edit"`，另加 `images: ["file-id", …]`、可选 `mask`；上传可先 `POST /api/uploads` 或 multipart 一次提交。

---

## 4. 服务端关键实现点

1. **任务表**（SQLite）  
   `id, type, status, params_json, error, created_at, updated_at, finished_at`
2. **结果表**（或文件约定）  
   `task_id, file_path, width, height`
3. **taskRunner**  
   收到任务 → 调 `imageApi` → 存图 → 更新状态；进程内串行/有限并发（如同时 2 个上游请求）。
4. **imageApi 双协议**  
   - OpenAI Images：generate JSON；edit multipart  
   - 兼容扩展：统一 generations，edit 用 `image`  
5. **超时**  
   上游 receive ≥ 10 分钟；Express 对 `/api/tasks` 提交快速返回，不等出图。
6. **安全**  
   `.env` 存 Key；不把 Key 回传前端；上传做类型/大小限制；`/files` 防路径穿越。

---

## 5. 前端关键实现点

1. **Generate / Edit 两页**（Tab 或路由）  
2. **提交后** 拿 `task_id`，每 2–3s 轮询直至终态  
3. **刷新/关页** 可通过 `localStorage` 记住最近 `task_id`，回站继续查  
4. **下载** 用 `<a download href="/files/...">`  
5. **深色模式** localStorage  
6. **上传** `input[type=file]` 多选；编辑页可选遮罩（协议为兼容时隐藏）

---

## 6. 服务端配置：多套 API 配置集

**原则：密钥进 `.env`，结构进 `config.json`。** `.env` 不入库；`config.json` 只含非敏感字段，密钥用 `${ENV_NAME}` 引用。

### 6.1 `server/.env`（密钥与部署项）

```bash
PORT=3000
DATA_DIR=./data

# 仅放密钥 / 敏感值，名称与 config.json 里的 ${...} 对应
API_KEY_JENIYA=sk-xxxx
API_KEY_QWEN=sk-yyyy
```

### 6.2 `server/config.json`（多 Profile，非密钥）

```json
{
  "activeProfileId": "qwen",
  "profiles": [
    {
      "id": "jeniya",
      "name": "Jeniya",
      "baseUrl": "https://jeniya.cn",
      "apiKeyEnv": "API_KEY_JENIYA",
      "protocol": "openai",
      "defaultModel": "gpt-image-2",
      "models": ["gpt-image-2", "z-image-turbo"],
      "defaultSize": "1024x1024",
      "defaultCount": 1
    },
    {
      "id": "qwen",
      "name": "千问兼容",
      "baseUrl": "https://maas.qianwenaiapi.com/compatible-mode",
      "apiKeyEnv": "API_KEY_QWEN",
      "protocol": "compatible",
      "defaultModel": "qwen-image-3.0-pro",
      "models": ["qwen-image-3.0-pro", "qwen-image-3.0"],
      "defaultSize": "1024x1024",
      "defaultCount": 1
    }
  ]
}
```

| 字段 | 说明 |
|------|------|
| `apiKeyEnv` | 对应 `.env` 变量名，**禁止**在 config.json 写明文 Key |
| `protocol` | `openai` \| `compatible`（对应功能清单双协议） |
| `models` | 前端模型下拉；`defaultModel` 须在列表内或为自由文本 |
| `activeProfileId` | 当前默认配置；前端可传 `profileId` 覆盖 |

### 6.3 加载规则

1. 启动时读 `config.json` + `dotenv`  
2. 解析 `apiKeyEnv` → 进程内得到 `apiKey`（不写回文件、不回传前端）  
3. 缺失对应环境变量的 profile：标记不可用，提交任务时返回明确错误  
4. 改配置：改文件后重启进程（首版不做热更新）  

### 6.4 前端如何选配置

- `GET /api/profiles` → 只返回 `id/name/models/defaultModel/defaultSize/protocol`（**无 Key**）  
- 提交任务可带 `profileId`；缺省用 `activeProfileId`  

### 6.5 文件与仓库

| 文件 | 入库 | 说明 |
|------|------|------|
| `server/config.json` | ✅ 可入库（无密钥） | 团队共享结构 |
| `server/.env` | ❌ gitignore | 仅本机/服务器 |
| `server/.env.example` | ✅ | 只列变量名占位 |

单套上游时可只留一个 profile；密钥仍只放 `.env`。

---

## 7. 演进路径

| 阶段 | 做法 |
|------|------|
| 首版 | Express 单进程 + SQLite + 有限并发；Vite SPA 静态托管 |
| 并发升高 | 任务队列（BullMQ + Redis），多 worker 调上游 |
| 多实例/多用户 | PostgreSQL；对象存储（S3/OSS）存图；可选登录 |
| 体验增强 | SSE/WebSocket 推送状态；任务历史分页 |

---

## 8. 与原 Flutter 客户端差异（摘要）

| 原客户端 | Web 方案 |
|----------|----------|
| Dio 同步长请求 | 服务端任务 + 轮询 |
| 前台服务/托盘 | 不做 |
| SharedPreferences | 服务端配置 + localStorage |
| 保存到相册 | 浏览器下载 |
| 客户端持 API Key | Key 仅服务端 |
