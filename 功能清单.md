# Z Ai 功能清单（网页版）

> 基于原 Flutter 客户端能力整理，**已剔除网页无法实现的原生功能**，作为 Node 服务端 + 网页前端的功能基线。
> 整理日期：2026-09-16
> 技术栈：**Express + SQLite + Vite + Vue 3**（详见同目录《技术选型.md》）

---

## 1. 产品定位

AI 图像**生成**与**编辑** Web 应用。浏览器访问服务端页面，由 **Node 服务端**代理上游图像 API，用户文生图、传图改图，并预览/下载结果。

**核心用户路径**

1. （可选）选择/确认 API 配置  
2. 文生图：输入提示词 → 选尺寸/数量/模型 → 生成 → 预览/下载  
3. 图编辑：上传源图（+ 可选遮罩）→ 输入编辑描述 → 编辑 → 预览/下载  

---

## 2. 功能模块一览

| 模块 | 状态 | 说明 |
|------|------|------|
| 文生图 | ✅ | 提示词 → 生成 1–N 张图 |
| 图编辑 / 图生图 | ✅ | 1–N 张源图 + 描述 → 改图 |
| 遮罩编辑 | ✅（仅 OpenAI Images 协议） | 指定编辑区域，仅对第一张图生效 |
| 大图预览 | ✅ | 全屏、缩放、多图切换、下载 |
| 结果管理 | ✅ | 网格展示、清空、单项下载 |
| 任务提交与查询 | ✅ | 提交后轮询任务状态（长请求不绑页面） |
| API 配置 | ✅ | 服务端配置；前端可选切换配置集 |
| 深色模式 | ✅ | 明暗主题，localStorage 记忆 |
| 取消任务 | ✅ | 对服务端任务发起取消（尽力而为） |

**网页不做（原生独有）**

| 功能 | 原因 |
|------|------|
| 系统托盘 | 浏览器无系统托盘 |
| 前台服务保活 | 浏览器无 Android 前台服务；长任务改由服务端承担 |
| 原生通知栏进度 | 可用 Web Notification（可选），非必须 |
| 相册写入（gal） | 改为浏览器下载 |
| Windows 托盘四态图标 | 同上，不适用 |

---

## 3. 文生图（Generate）

| 功能 | 细节 |
|------|------|
| 提示词输入 | 多行输入，上限 **1000** 字；即时清除按钮 |
| 尺寸 | OpenAI：1024² / 1536×1024 / 1024×1536 / 2048² / 2048×1152 / 4K 横竖 / auto |
| 尺寸（兼容协议） | 同上去掉 4K，另加 1152×2048；面积约 ≤2048² |
| 生成数量 | OpenAI：1–10；兼容协议：1–6 |
| 模型 | 下拉可选；服务端配置的自定义列表或内置 |
| 请求附加字段 | OpenAI：`quality=auto`、`format=png`；兼容协议：不发送这两项 |
| 取消 | 进行中可取消 |
| 进行中 UI | 进度指示 + 可离开页面（任务在服务端继续） |
| 结果 | 网格追加；点击大图；可下载；可清空（需确认） |
| 错误 | 错误提示；失败可重新提交同一参数 |

**服务端调用**：`POST {baseUrl}/v1/images/generations`（JSON）

---

## 4. 图编辑（Edit）

| 功能 | 细节 |
|------|------|
| 源图上传 | 多张；`<input type=file>` / 拖拽；缩略图、单张删除、清空 |
| 源图限制 | 建议 384–2048、≤10MB（上游约束）；服务端校验 |
| 遮罩图 | 可选；**仅 OpenAI Images**；兼容协议隐藏入口 |
| 编辑描述 | 上限 **32000** 字 |
| 尺寸 / 数量 / 模型 | 同文生图规则 |
| 请求协议 | **OpenAI Images**：`/v1/images/edits` multipart（`image` + 可选 `mask`） |
| 请求协议 | **兼容扩展**：`/v1/images/generations`，JSON `image`（URL 或 Base64） |
| 清空源图 | 一并清空遮罩 |
| 结果 / 取消 / 错误 | 同文生图 |

---

## 5. 大图预览与下载

- 全屏深色底；点击切换 UI 浮层  
- 滚轮/双指缩放（约 0.5×–4×）  
- 多图左右切换 + 圆点页码 + `n / total`  
- **下载**当前图到本地（浏览器下载，替代原「保存到相册」）  
- 加载失败占位  

---

## 6. 结果网格

- 网格展示本次会话已生成/已编辑图片（追加式）  
- 单张：进预览、下载  
- 清空：二次确认  
- （可选）历史任务结果列表由服务端任务表提供  

---

## 7. 任务模型（服务端）

| 项 | 说明 |
|----|------|
| 提交 | `POST /api/tasks`：type=`generate`\|`edit`，prompt、model、size、n、images[]、mask? |
| 查询 | `GET /api/tasks/:id`：状态 `queued` / `running` / `succeeded` / `failed` / `canceled` |
| 取消 | `POST /api/tasks/:id/cancel`（尽力而为） |
| 结果 | 成功后返回图片 URL 列表；前端轮询（2–3s）或 SSE/WebSocket |
| 持久化 | 任务落库/落盘，刷新、关页、换设备可继续查 |
| 上游 Key | **仅存服务端 `.env`**；多 Profile 结构在 `config.json`（`apiKeyEnv` 引用），前端不接触密钥 |

生图 3–4 分钟：长请求在 Node 进程内完成，**关掉浏览器标签不影响出图**。

---

## 8. API 配置与协议

### 8.1 配置（服务端为主）

| 字段 | 说明 |
|------|------|
| 上游 Base URL | 默认 `https://jeniya.cn`；千问示例 `https://maas.qianwenaiapi.com/compatible-mode` |
| API Key | **仅 `.env`**，config 里用 `apiKeyEnv` 引用 |
| API 协议 | `OpenAI Images` / `OpenAI 兼容扩展`（JSON image） |
| 默认模型 | 如 `gpt-image-2`、`qwen-image-3.0-pro` |
| 可选模型列表 | 供前端下拉 |
| 默认尺寸 / 数量 | 随协议约束 |

**多配置集**：`server/config.json` 的 `profiles[]` 支持 jeniya / 千问等多套并存；`GET /api/profiles` 给前端切换，提交任务可带 `profileId`。详见《技术选型.md》§6。

### 8.2 请求协议差异

| 项 | OpenAI Images | OpenAI 兼容扩展 |
|----|---------------|-----------------|
| 文生图 | `/v1/images/generations` + quality/format | 同端点，无 quality/format |
| 图编辑 | `/v1/images/edits` multipart | `/v1/images/generations` + `image` |
| 遮罩 | 支持 | 不支持 |
| n 上限 | 10 | 6 |
| 4K 尺寸 | 有 | 无 |

### 8.3 上游响应解析

- `data[].url` 或 `data[].b64_json`  
- 兜底 `choices[].message.content` 当 b64  

### 8.4 超时

- 服务端 → 上游：连接 30s · 接收 **10 分钟**  
- 浏览器 → 服务端：查询接口短超时即可（任务在服务端跑）  

---

## 9. 界面与导航

- 双 Tab / 双页：文生图 · 图编辑（可叠设置、任务/历史）  
- 明暗主题（手动开关，localStorage）  
- 响应式布局（桌面为主，兼容窄屏）  
- 表单校验：提示词非空；编辑至少一张源图  
- 错误横幅 / Toast 反馈  

---

## 10. 浏览器能力边界

| 能力 | 网页做法 |
|------|----------|
| 选图上传 | `<input type=file>`、拖拽；可选 `getUserMedia` 拍照（非必须） |
| 保存图片 | `download` / `<a download>` |
| 配置记忆 | localStorage（主题、前端偏好）；上游 Key 不进浏览器 |
| 长任务 | 服务端跑完；前端可关页后靠任务 ID 回看 |
| CORS | 浏览器不直连上游，统一走 Node 同源代理 |

---

## 11. 缓存

| 项 | 说明 |
|----|------|
| 服务端图片存储 | 任务结果落盘/对象存储，供预览与下载 |
| 内存缓存 | 可选（热门缩略图） |
| 浏览器 | HTTP 缓存 / 可选 Cache API；无原生 LRU 逻辑 |

---

## 12. 校验与错误

- 提示词非空；编辑需至少一张源图  
- 文件类型/大小校验（服务端 + 前端）  
- 取消与失败文案分离  
- 上游错误透传为可读 message  

---

## 13. API 面汇总

**浏览器 → Node**

| 端点 | 方法 | 用途 |
|------|------|------|
| `/api/tasks` | POST | 提交生成/编辑任务 |
| `/api/tasks/:id` | GET | 查询状态与结果 |
| `/api/tasks/:id/cancel` | POST | 取消 |
| `/api/tasks` | GET | （可选）历史列表 |
| `/files/:id` | GET | 预览/下载图片 |

**Node → 上游**

| 端点 | 方法 | 用途 |
|------|------|------|
| `/v1/images/generations` | POST JSON | 文生图；兼容协议下兼作图生图 |
| `/v1/images/edits` | POST multipart | OpenAI 图编辑（+mask） |
| 结果 URL | GET | 下载图片字节并转存 |

---

## 14. 服务端重构映射

| 原客户端 | 网页 + Node |
|----------|-------------|
| 文生图 / 图编辑 | 前端表单 → `POST /api/tasks` |
| 客户端内长 HTTP | Node 调上游，任务状态机 |
| 保存到相册 | 浏览器下载 |
| SharedPreferences | 服务端配置 + 前端 localStorage |
| 网络日志面板 | （可选）服务端日志 |
| 遮罩 / 双协议 | 保留在 Node 适配层 |
| 托盘 / 前台服务 | **删除**（不适用） |

---

## 15. 已知缺口 / 非目标

**缺口**
- 多用户账号（内网单人可省略登录）  
- 历史任务分页 UI（接口可先留）  
- 兼容协议扩展参数 UI（`seed` / `prompt_extend` 等，可后加）  

**非目标**
- 系统托盘、前台服务、原生通知栏  
- 移动端原生安装包（可用响应式网页）  
- 视频 / 音频生成  

---

## 16. 术语

| 术语 | 含义 |
|------|------|
| 任务 / Task | 一次生成或编辑的服务端异步作业 |
| OpenAI Images | `/images/generations` + `/images/edits` multipart |
| OpenAI 兼容扩展 | 统一 `/images/generations`，图编辑用 JSON `image`（千问等） |
| 文生图 / T2I | 仅提示词生成 |
| 图编辑 / I2I | 源图（+遮罩）+ 描述改图 |
