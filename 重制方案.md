# Z Ai — 代码重制方案（Rework Plan）

> 基于全量代码诊断（数据/网络层 + UI/状态/平台层）整理。
> 目标：**架构现代化 + 技术栈优化**，保留全部现有功能与设计，不做推倒重写。
> 基线：约 6635 行 Dart / 56 文件；Flutter 3.44.2 / Dart 3.12.2；`dart analyze` 38 issues（0 error）。

---

## 执行进度（2026-08-07 更新）

| 阶段 | 状态 | 说明 |
|---|---|---|
| 决策：Web 去留 | ✅ 已定 | **放弃 Web**，保留 iOS/Android/Windows/macOS/Linux |
| Phase 0 止血 | ✅ 完成 | 7 项全部落地 |
| Phase 1 · 去 Web + 清死代码 | ✅ 完成 | 删 10 个文件 |
| Phase 1 · 组件库化 | ✅ 完成 | 对话框/反馈统一、假保存图标修复、平台抽象合并 |
| Phase 1 · 拆分巨型页面 | ✅ 完成 | 新增 12 个共享组件文件（见下） |
| Android 构建验证 | ✅ 完成 | `build apk --release` 成功，产物 app-release.apk (52.5MB) |
| Phase 2 架构现代化 | ⏳ 待决策 | 需先拍板 codegen 与 i18n |
| Phase 3 测试收尾 | ⏳ 待做 | 见下方「已知环境阻塞」 |

**静态检查：`dart analyze` 38 → 0 issues。**

### 已知环境阻塞（影响 Phase 3）
- `flutter` / `dart` 的 shell wrapper 在**沙箱内**静默失败（无输出 + exit 1），
  因其需写 `D:\flutter\bin\cache\lockfile` 且被宿主锁 + 写保护双重拦截。
  **绕过：** 用「直接 snapshot」配方 **+ `dangerouslyDisableSandbox`（脱沙箱）** 即可跑通
  （用户开权限后验证 `build apk --release` 成功）。配方见下。
- `flutter test` 当前仍待验证：需在脱沙箱环境下用同一配方运行（lockfile 问题解决后应可跑，
  但 `rm` 受 safe-delete fail-closed 影响，测试产物清理需注意）。

---

## 0. 诊断结论摘要

### 0.1 两个结构性根因
1. **`settings_provider.dart` 是 God File（310 行）**：同时承担"DI 装配 + 全局状态机 + 配置分发"。其中 `apiClientProvider` 用 `ref.watch(settingsProvider)` 监听**整个状态对象**，导致任意设置变更都会级联重建整棵业务 Provider 树 → **用户已生成的图片、已选源图、聊天记录被清空**（改画布尺寸、点一下数量 ±1、切换深色模式都会触发）。
2. **大量"写好了但从未接线"的死代码（约 400+ 行）**：`core/error/app_error.dart`（已设计好 sealed 错误）、`core/state/base_state.dart`（138 行 OperationState）、`ImageStorage.create()`、`getFromCache()`、`BackgroundOperationMixin`、`utils/notification_service*.dart`（3 个）、`widgets/common/loading_indicator.dart`、`cached_network_image`/`workmanager`/`window_manager` 依赖。抽象写了却没接，反而制造了"两套并存"的混乱。

### 0.2 用户可见的功能性 Bug（优先级最高）
- **切深色模式 → 顶/底栏白底白字**（`home_page.dart:48/81/88/90` 硬编码白色玻璃，深色前景叠上去）。
- **"移除遮罩"按钮完全无效**（`edit_provider.dart:53` 的 `??` 语义 copyWith 导致 `clearMaskImage()` 是 no-op，遮罩仍被提交，产出错误结果）。
- **提示词清除按钮三态错乱**（输入框无 `onChanged`，`state.prompt` 只在生成时才赋值）。
- **图片内存 OOM 定时炸弹**：`Image.memory` 全线路缺 `cacheWidth` + Provider 无 `autoDispose` + `IndexedStack` 三页常驻 + 支持 4K×10 张。
- **API Key 明文泄漏到网络日志 UI 并可复制**（`Authorization: Bearer <完整 Key>` 被日志拦截器抓到并完整渲染）。
- **磁盘缓存 100% 失效**：`imageStorageProvider` 注入了空目录 `Directory('')`，保存异常被静默吞掉。
- **"取消"被当作"错误"弹给用户**（全项目无一处区分取消与失败）。

### 0.3 架构/质量债务
- `generate_page`(422) 与 `edit_page`(605) **复制粘贴率 >70%**（含 83 行逐字符相同的 `_buildBasicParams`）；已存在可消除重复的 `BackgroundOperationMixin` 却 0 引用。
- `settings_page`(651) 的 `build()` 独占 428 行；5 处手写 `AlertDialog`（已有 `showConfirmDialog` 却没用）。
- 同一"前台通知"能力有 **4 条并行调用路径**（UI 直调静态类 / Provider 直调 MethodChannel / `ForegroundServiceImpl` / 从未实例化的 `NotificationServiceImpl`），且 `foreground_service_impl.dart:10` 有挂在不存在接口方法上的非法 `@override`。
- **"支持 Web"被 9 处无条件 `import 'dart:io'` 彻底证伪**（含核心依赖 `platform_capabilities.dart`），Web 实际编译不过。
- 两套同名错误模型（`app_exception.dart` vs `app_error.dart`）并存冲突；状态类无 `==`，导致无效 rebuild。
- 依赖了未启用的 lint 强化规则；`analysis_options.yaml` 的 `rules:` 全被注释。

---

## 1. 技术栈调整建议（总览）

| 维度 | 现状 | 建议 | 说明 |
|------|------|------|------|
| 状态管理 | `StateNotifierProvider` + 手写 `isLoading/error/data` | **Riverpod codegen `@riverpod` + `AsyncNotifier`** | Riverpod 3.0 已移除 `StateNotifierProvider`；`AsyncValue` 天然带 `isRefreshing`/保留旧值语义 |
| 模型/状态类 | 全手写、缺 `==`/`copyWith` | **freezed + json_serializable** | 一次性根治 copyWith null 语义地狱、消除手写出错 |
| 错误处理 | 裸异常 + 字符串 `contains` 嗅探 | **统一 sealed `AppError` + `Result<T, Failure>`** | 扶正现有 `app_error.dart`，删除 `app_exception.dart`；取消提升为独立 variant |
| 路由 | 手写 `Navigator.push` | **go_router + `StatefulShellRoute.indexedStack`** | 解决 Tab 保活+转场、Web 深链、为"从通知进结果页"打底 |
| 本地存储(对话) | SharedPreferences 全量 JSON 单 key | **drift** | 增量写入、分页、Stream 响应式、可迁移 |
| API Key 存储 | SharedPreferences 明文 | **flutter_secure_storage** | Keychain/Keystore 加密 |
| 主题/设计系统 | 明暗两套 95% 重复、硬编码色 | **`ColorScheme.fromSeed` + `ThemeExtension` 设计 token** | `AppSpacing`/`AppRadius`/`AppSurfaces` |
| 平台抽象 | 4 条并行路径 + 静态可变单例 | **interface + 条件导入 + Riverpod 注入** | 统一单一入口，可 mock、可测试 |
| 静态检查 | 仅 flutter_lints | **开启 strict-casts / avoid_dynamic_calls / unused→error** | 防回归 |
| 依赖瘦身 | 3 个死依赖 + 多个死文件 | **删除** | 净删约 400 行 |

**明确不做的**：推倒重写；引入 `dartz`（可读性差）；引入 **retrofit**（端点仅 3 个且含 multipart + 动态 baseUrl，收益为负）。

---

## 2. 分阶段执行计划

### Phase 0 — 止血（低风险、高价值，先做，约 1–2 天）✅ 已完成
目标：修掉用户能实际感知到的严重缺陷，**不触碰结构**。

- **P1/S1**：`apiClientProvider` 改为 `ref.watch(settingsProvider.select((s) => s.activeProfile()))` + `ref.onDispose(() => dio.close())`。**根治"改设置清空图片/聊天"**。
- **P2/A8**：修 `clearMaskImage`（freezed 或修正 copyWith 语义）。
- **P14/T2（部分）**：修深色模式顶/底栏白底白字（引入 `AppColors` + `ThemeExtension.glass`）。
- **P27/U5**：修提示词清除按钮三态（加 `onChanged` + `ValueListenableBuilder`）。
- **P30/U4（部分）**：所有 `Image.memory` 加 `cacheWidth/cacheHeight`（5 行级，立即防 OOM）。
- **A4/A5**：网络日志脱敏（`Authorization → Bearer ***`）+ body 截断到 2KB（避免 OOM + Key 泄露）。
- **A7/S4（部分）**：把"取消"提升为独立 `AppFailure.cancelled`，不再弹错误横幅。

### Phase 1 — 结构重构（机械拆分，低风险，约 2–3 天）🔶 进行中
- ✅ **U2/P9–P11**：拆分巨型页面 — 已完成。
  - `widgets/params/` 5 个共享组件：`prompt_field.dart`(PromptField)、`size_count_selector.dart`(SizeAndCountSelector)、`model_selector.dart`(ModelSelector)、`submit_button.dart`(SubmitButton)、`result_section.dart`(ResultSection)。generate/edit 的 `_buildPromptInput`/`_buildBasicParams`/`_buildAdvancedParams`/`_buildGenerateButton`/`_buildEditButton`/`_buildResults` 全部替换为调用，复制粘贴基本消除。
  - `widgets/chat/chat_bubble.dart`：`ChatAvatar`/`ChatBubble`/`TypingBubble` 三件套，chat_page 的 `_buildMessageBubble` 拆分收敛。
  - `widgets/settings/` 5 个 section：`api_config_section.dart`(自带 Form+控制器+增删/保存对话框，用 `ref.listenManual` 按「可编辑字段签名」同步文本框)、`appearance_section.dart`、`tray_section.dart`、`data_section.dart`、`about_section.dart`。settings_page 由 588 行收敛为 ~30 行 ConsumerWidget。
  - `utils/background_error.dart`：抽取 `isBackgroundInterruptedError`，消除 generate/edit 生命周期逻辑重复。
- ✅ **U3/P12/P26**：组件库化。settings_page 4 处手写 AlertDialog 已迁移（第 5 处在 chat_page，实为死代码，已删）；
  新增 `showTextInputDialog`（顺带修掉 controller 泄漏）与 `showConfirmDialog(isDestructive:)`；
  `result_grid` 假保存图标 → 真实 `onItemSave` 按钮；新增 `widgets/common/feedback.dart` 统一 SnackBar/复制/保存反馈。
- ✅ **P-2**：合并 `IForegroundService`/`INotificationService`（后者是从未实例化的完全重复抽象，已删）；
  `showCompleted` 并入前者；补 `requestPermission` 到接口以修非法 `@override`；bootstrap 改持接口类型。
  ⏳ 剩余：provider 注入、禁止页面直调 `ForegroundService`。
- ✅ **P-1（决策后）**：已放弃 Web，删 10 个死文件。
- ✅ **P-5**：依赖瘦身。**注意：`workmanager` / `window_manager` 不可删** ——
  已被 Android `GeneratedPluginRegistrant` 与 Windows `generated_plugin_registrant.cc` 原生注册引用，
  故实际仅移除 `cached_network_image`，净删行数少于原估的 ~400。

### Phase 2 — 架构现代化（较高成本，约 3–5 天）
- **S2**：`generate/edit/chat/network_log/settings` 迁移到 `@riverpod` `AsyncNotifier`；删除 `StateNotifierProvider` 与手写三元组。
- **S3**：状态类 + 模型 freezed 化（正确的 `==`/`copyWith`）。
- **S4/E**：统一到 sealed `AppError`，删 `app_exception.dart`；UI 用 `switch` 替代 `contains` 字符串嗅探。
- **U1**：引入 go_router + `StatefulShellRoute.indexedStack`，删无效 `AnimatedSwitcher`。
- **存储升级**：对话历史 → drift；API Key → flutter_secure_storage。
- **T1/T2**：主题去重 + 设计 token（`AppSpacing`/`AppRadius`/`AppSurfaces` ThemeExtension）。
- **E1**：收紧 `analysis_options.yaml`。

### Phase 3 — 可测试性与收尾（约 2 天）
- **E2**：补 Widget/Unit 测试（mocktail + http_mock_adapter）；优先 `SizeAndCountSelector`、`ResultGrid`、`ErrorBanner`、`ChatBubble`、Repository。
- 验收：`dart analyze` 0 issue；Android/iOS/Windows/（若保留）Web 构建通过。

---

## 3. 关键决策点（需你拍板）

1. ~~**Web 去留？**~~ ✅ **已拍板：放弃 Web。** 已执行完毕。
2. **现代化范围？** 全套（`@riverpod` + freezed + go_router + drift + secure_storage）vs 最小必要（仅 Phase 0/1 + 轻量错误处理统一）？
3. **是否接受 Riverpod codegen 迁移**（引入 build_runner 代码生成）？建议在 Phase 1 页面拆分完成后做，降低冲突面。
4. **是否要做 i18n？** 当前中文硬编码上百处。若无出海计划，至少先抽 `AppStrings` 常量类。

---

## 4. 反目标（Anti-goals）
- 不推倒重写（保留功能与设计文档对齐的成果）。
- 不引入 `dartz`、不引入 `retrofit`。
- 不在 Phase 2 之前做 codegen 迁移（避免与巨型页面改造冲突放大回归风险）。

---

## 5. 工作量估算
| 阶段 | 工期 | 风险 |
|------|------|------|
| Phase 0 止血 | 1–2 天 | 低 |
| Phase 1 结构重构 | 2–3 天 | 低 |
| Phase 2 架构现代化 | 3–5 天 | 中 |
| Phase 3 测试收尾 | 2 天 | 低 |
