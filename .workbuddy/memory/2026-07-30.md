# 2026-07-30

## 编译验证
- 对前一日修复（结果标题重复 / 清除无效 / 后台保活 / 切tab打断文生图）做 release 编译
- `flutter pub get` OK；`flutter build apk --release` 成功，产物 `build/app/outputs/flutter-apk/app-release.apk`（52.5MB）
- 仅有无害 warning：workmanager_android 用到 Kotlin Gradle Plugin，未来 Flutter 版本需注意升级插件

## 关键改动回顾（前一日会话延续）
- home_page.dart：删除 `IndexedStack` 的 `key: ValueKey(_currentIndex)`，修复切 tab 销毁子页面导致文生图请求被 cancel 的问题
- result_grid.dart：新增 `title` 参数，页面不再自建重复标题
- generate_provider/edit_provider：新增 `clearResults()`，页面清除按钮改调它
- generate_provider/edit_provider：集成此前写好但未调用的 `NativeForegroundService` 实现真前台服务保活
- foreground_service.dart：统一通知渠道 ID
- AndroidManifest.xml：权限 `FOREGROUND_SERVICE_SPECIAL_USE` → `FOREGROUND_SERVICE_DATA_SYNC`
- GenerateForegroundService.kt：Android 14+ 显式传入 `FOREGROUND_SERVICE_TYPE_DATA_SYNC`
