
## 上传 APK 到 Cloudreve
- 找到编译产物：/d/work/z_ai_img/build/app/outputs/flutter-apk/app-release.apk（55MB，release 版，7/30 构建）。build/app/outputs/apk/release/app-release.apk 内容完全相同。
- 使用 cloudreve-v4-upload skill 上传到 pan.cdn.ad（S3 策略，hk 区域），成功。
- 执行命令需用 Windows 绝对路径（Git Bash 的 /c/... 路径会被错误解析成 d:/c/...）；node 用托管版 C:/Users/zheng/.workbuddy/binaries/node/versions/22.22.2/node.exe。
- 产物下载链为 S3 临时签名直链，约 1 小时（3599s）有效，过期需重跑生成新链。
