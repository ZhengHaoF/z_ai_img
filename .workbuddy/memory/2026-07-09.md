# 2026-07-09

- 成功编译 Windows 桌面应用。修复了 Visual Studio C++ 编译错误（experimental/coroutine 弃用警告），在 `windows/CMakeLists.txt` 的 `APPLY_STANDARD_SETTINGS` 函数中添加了 `_SILENCE_EXPERIMENTAL_COROUTINE_DEPRECATION_WARNINGS` 预处理器定义。
