# Z Ai 项目 Bug 修复说明

> 修复日期：2026-09-16
> 验证：`flutter analyze` 无问题（No issues found）

---

## 一、高优先级修复

### 1. macOS 沙盒网络权限
**问题**：`macos/Runner/DebugProfile.entitlements` 和 `Release.entitlements` 缺少 `com.apple.security.network.client`，沙盒应用无法发起任何出站网络请求，macOS 端核心 API 调用必然失败。

**修复**：两个 entitlements 文件均添加：
```xml
<key>com.apple.security.network.client</key>
<true/>
```

### 2. 托盘图标资源为空
**问题**：`assets/icons/` 目录为空，但 `system_tray.dart` 引用了 4 个托盘图标，开启托盘开关后 `setIcon` 必然抛异常。

**修复**：
- 新增 `tool/generate_tray_icons.py`（Pillow 脚本），生成 4 种状态的图标：
  - `tray_idle.png / tray_loading.png / tray_completed.png / tray_error.png`（32x32，macOS/Linux）
  - 对应 `.ico` 文件（16~256 多尺寸，Windows）
- `system_tray.dart` 按平台选择格式：`Platform.isWindows ? 'ico' : 'png'`

### 3. Android 前台服务幽灵通知
**问题**：`GenerateForegroundService.onStartCommand` 返回 `START_STICKY`，进程被杀后系统用 null intent 重启服务，显示无意义的「AI 任务进行中」通知。

**修复**：
- 返回值改为 `START_NOT_STICKY`
- 增加 `intent == null` 分支：直接停止服务并移除前台通知

### 4. 「清除所有设置」清不干净
**问题**：`SettingsNotifier.clearAll()` 未移除 `isDarkMode` / `showTrayIcon` 持久化键，重启后会恢复旧值。

**修复**：`clearAll()` 补删两个键：
```dart
await _prefs.remove('isDarkMode');
await _prefs.remove('showTrayIcon');
```

---

## 二、中优先级修复

### 5. 缓存层三个问题
- **`clearCache()` 死代码 + 只清内存** → 改为 `Future<void> clearCache()`，同时清内存与磁盘
- **`_imageStorage.save()` 未 await** → 用 `unawaited(...then<void>...catchError)` 防 unhandled async exception
- **`_evictOldest()` O(n²)** → 先一次遍历统计总大小，再按需删除，降为 O(n log n)

### 6. 网络日志并发时间戳串号
**问题**：`_requestTimestamps` 以 URL 为 key，并发同 URL 请求会互相覆盖。

**修复**：改为自增 ID，写入 `options.extra['request_id']`，响应/错误时按 ID 取回。

### 7. HomePage 切换动画失效
**问题**：`AnimatedSwitcher` 包 `IndexedStack` 无 key，child 类型不变导致动画不触发。

**修复**：新增 `FadeIndexedStack`（`TickerProviderStateMixin` + `FadeTransition` + `IndexedStack`），保留页面状态并实现淡入淡出。

### 8. 编辑页状态问题
- `clearSourceImages()` 清空源图后**遮罩仍保留** → 同时清空遮罩
- `clearMaskImage()` 构造新状态时**顺带清掉错误横幅** → 显式保留 `error`

### 9. 清理未使用依赖和多余权限
- 移除 pubspec 中 `workmanager` / `window_manager`（零引用）
- AndroidManifest 删除 `RECEIVE_BOOT_COMPLETED` / `WAKE_LOCK`（workmanager 专用）
- 保留 `FOREGROUND_SERVICE` / `POST_NOTIFICATIONS` / `FOREGROUND_SERVICE_DATA_SYNC`（原生前台服务需要）

### 10. Android 保存图片权限逻辑
**问题**：`WRITE_EXTERNAL_STORAGE` maxSdkVersion=28 与 gal 要求的 29 不一致；Android 10 (API 29) 保存相册需要 `requestLegacyExternalStorage`；Android 13+ 请求废弃的 storage 权限。

**修复**：
- Manifest：`WRITE_EXTERNAL_STORAGE` maxSdkVersion 28→29，添加 `android:requestLegacyExternalStorage="true"`
- `image_utils.dart`：改用 gal 自身的 `Gal.hasAccess(toAlbum: true)` / `Gal.requestAccess(toAlbum: true)`（内部已处理各版本差异），移除手动 permission_handler 逻辑

---

## 三、改动文件清单

| 文件 | 改动 |
|------|------|
| `macos/Runner/DebugProfile.entitlements` | + `network.client` |
| `macos/Runner/Release.entitlements` | + `network.client` |
| `assets/icons/` | 新增 8 个托盘图标（4 png + 4 ico） |
| `tool/generate_tray_icons.py` | 新增图标生成脚本 |
| `lib/utils/system_tray.dart` | 图标路径按平台区分 |
| `android/.../GenerateForegroundService.kt` | `START_NOT_STICKY` + null intent 处理 |
| `lib/providers/settings_provider.dart` | clearAll 补删键 |
| `lib/repositories/image_repository.dart` | clearCache 异步化、save 防 unhandled |
| `lib/core/storage/image_storage.dart` | 淘汰算法 O(n²)→O(n log n) |
| `lib/core/network/base_http_client.dart` | 请求 ID 化 |
| `lib/pages/home_page.dart` | FadeIndexedStack |
| `lib/providers/edit_provider.dart` | clearSourceImages/clearMaskImage 状态修复 |
| `pubspec.yaml` | 移除 workmanager、window_manager |
| `android/app/src/main/AndroidManifest.xml` | 权限调整 + requestLegacyExternalStorage |
| `lib/utils/image_utils.dart` | 改用 gal 权限 API，移除多余 import |

---

## 四、验证结果

- ✅ `flutter pub get` 成功（workmanager/window_manager 及其传递依赖已移除）
- ✅ `flutter analyze`：**No issues found**
- ⚠️ `flutter build apk --debug` 因网络 TLS 握手失败无法下载依赖（**非代码问题**，网络恢复后即可构建）

> 注：历史构建日志中曾有 `workmanager_android 应用旧 Kotlin Gradle Plugin` 的警告，移除该依赖后此警告已消除。